---
name: Build Helm charts bundle

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - "*"
      - "**"
      - "*/*"

jobs:
  helm-repository-build:
    uses: obervinov/_templates/.github/workflows/helm.yaml@v1.0.11
